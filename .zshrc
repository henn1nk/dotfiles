if [[ -s "${ZDOTDIR:-$HOME}/.zprezto/init.zsh" ]]; then
  source "${ZDOTDIR:-$HOME}/.zprezto/init.zsh"
fi

alias c="clear"
alias v="vim"
alias s="sudo"
alias root="/"
alias gam="git commit -am"
alias home="cd $HOME"
